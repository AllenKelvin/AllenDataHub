import { OrdersHistoryPage } from "./orders";

export default function TelecelOrdersPage() {
  return (
    <OrdersHistoryPage
      network="Telecel"
      title="Telecel Orders"
      description="Order history filtered to Telecel data bundles."
    />
  );
}
